using UnityEngine;
using System.Collections;

public class SetHaptic : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () 
    {
      //  m_currentTarget.GetComponent<PlayerGhost>().m_controller.TriggerHapticPulse(clampedLength);
	}
}
